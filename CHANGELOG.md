# 変更履歴

ccloop の各バージョンで、利用者から見て何ができるようになったか・何が直ったかを記録する。
新しいバージョンが上。内部の作り替えやテストの追加など、使っていて違いが分からない変更は載せていない。

「未リリース」は、まだ公開版に含まれていない変更である。使うには feature の参照バージョンではなく
この checkout の `lib/` を直接動かす必要がある(README の「開発(この基盤自体)」を参照)。

## 未リリース

### 追加

- 記録された決定を人間が承認する仕組みができた。セッションが下した判断は一覧ファイルに並び、
  人間がチェックを入れたものだけがアーカイブへ移る。目を通す前に決定が流れていくことがなくなった。
- `ccloop status` が、未承認の決定の件数と直近 3 件のプレビュー、承認のしかたを表示するようになった
  (`--json` の出力にも含まれる)。一覧ファイルでチェックを入れた決定は、アーカイブへ実際に移る前でも
  この表示から外れる。
- 複数のセッションが同時に決定を記録しても、一覧ファイルの衝突が自動で解消されるようになった。
  人間の判断に回るのは、同じ箇所を互いに書き換えた場合だけ。
- `ccloop status` が、インストール済みの ccloop とリポジトリ内のソースの食い違いを警告するように
  なった(ccloop 自身を開発している場合のみ。判定できないときは警告しない)。
- `ccloop status` に、ループ本体(`ccloop run`)が今も動いているかと、状態が最後に更新された時刻が
  出るようになった(`--json` の出力にも含まれる)。実行中のタスクが 0 件のとき、手が空いているだけ
  なのかループが止まっているのかを見分けられる。別のマシンで動かした状態を見ているなど判定できない
  ときは「不明」と出る。
- `ccloop status` で、ループ本体の生存記録が読めない(壊れている・アクセスできないなど)ときも
  「動いていません」ではなく「不明」と表示するようになった。記録ファイルが無い(起動していない)
  場合とは区別される。
- セッションが、この変更履歴を自分で更新するようになった。リポジトリに変更履歴のファイルがあり、
  使っていて違いが分かる変更を入れた場合だけ、未リリース節に 1 行足す。実装より変更履歴が遅れて
  古くなることがなくなる(ファイルが無いリポジトリでは何もしない)。
- `ccloop abandon <タスクID>` で、失敗したタスクに再挑戦させず断念できるようになった。断念したタスクは
  `ccloop status` の要対応一覧から消え、次回のローテーションでアーカイブへ退避される。気が変わったら
  `ccloop retry` で取り消せる。
- `ccloop doctor` が Claude にログインしているかを確認するようになった。未ログインのまま気づかずに
  ループを回してタスクが失敗し続ける事態を、実行前に検知できる(API キー等での認証は問題として
  扱わない)。
- `ccloop retry <タスクID>` を追加した。failed / blocked のタスクを、直前の失敗理由を表示したうえで
  再実行対象(status: ready)に戻せる。タスクファイルの frontmatter を手で編集する必要がなくなった。
- `ccloop status` / `ccloop watch` が実行ログを読む量に上限を設けた。記録が増えても表示が重くならず、
  上限にかかったときは費用の累計などが直近ぶんの集計であることを表示に明記する(記録そのものは
  削除・圧縮せずそのまま残る)。
- `ccloop status` が、設定ファイル(`.agent/config.json`)の版数が古いことを「要対応」に表示するように
  なった。以前はコマンド実行時の警告 1 行でしか気づけず、気づかないまま `ccloop run` が起動を
  拒否されるまで放置されがちだった。
- `ccloop status` の退避された衝突ブランチが、「main に取り込み済み(削除してよい)」と
  「main に未取り込み(要確認)」の 2 つに分かれて表示されるようになった。取り込み済みのブランチには
  削除コマンドが添えられ、そのまま実行すれば片付く。

### 修正

- 作業用のディレクトリを `ccloop` を介さず手で削除しても、次にそのタスクが実行されるときに
  自動で復旧するようになった。以前は管理情報だけが残ってしまい、`ccloop run` を再起動するまで
  タスクセッションが起動できなかった。
- 変更履歴のように「同じ節へ各ブランチが 1 行足す」ファイルの衝突が、解消しても次の試行で
  また起きる問題を直した。両方の項目を残す形で自動的に解決されるようになり、完成した成果が
  衝突の繰り返しだけで失敗扱いになることがなくなった。
- 衝突解消のセッションに、追記だけのファイルは両側を残すこと、担当タスクの記録ファイルは
  同じ場所へ書き足さないことが伝わるようになった。同じ衝突を何度も繰り返しにくくなる。
- `ccloop retry` / `ccloop abandon` でタスクの記録ファイルを書き換えたときに、ccloop が知らない
  項目(セッションが書いた更新日時など)が黙って消えていた問題を直した。書いた内容がそのまま残る。
- 時間切れやターン数の上限で打ち切られたタスクをやり直すとき、セッションに「まずタスクを小さく
  割ることを検討し、1 ラウンドぶんの成果を残して残りを新しいタスクにする」よう伝えるようになった。
  1 回では終わらない大きさのタスクが、同じ理由で上限回数まで打ち切られ続けることが減る
  (1 セッションの持ち時間は変わらない)。
- 大きな未コミット差分が退避に失敗したとき、作業場所を削除せず残すようにした(以前は差分が
  失われていた)。残った作業場所は `ccloop status` の「要対応」に表示される。
- 次の作業を探すセッションの起動に失敗しても、`ccloop run` 全体が落ちなくなった。失敗は
  そのセッション 1 件のクラッシュとして記録され、ループはそのまま次の周回へ進む。
- `ccloop run` を動かしたまま `.agent/claude-settings.json` へ権限を追記したり
  `.agent/PROMPT.local.md` を編集したりしても、次に始まるセッションから反映されるようになった。
  以前は `ccloop run` を再起動するまで反映されず、ドキュメントの説明と食い違っていた。
  あわせて、`.agent/claude-settings.json` が壊れていた場合に警告が出るようになった
  (以前は無言でその追記を無視していた)。
- git worktree の中で ccloop を実行しても、本体リポジトリと同じ実行状態を参照するようになった。
  以前は worktree ごとに別のリポジトリとして扱われ、無関係な状態が表示されていた。
- 確認事項(Human Review)への回答が検出されないことがある不具合を修正した。回答の判定は
  ファイル末尾のチェックボックスに一本化された。
- タスクファイルの status に想定外の値が書かれていると、そのタスクが `ccloop status` /
  `ccloop watch` の一覧からも進捗の分母からも黙って消えていた。除外されたファイルが要対応として
  表示されるようになった。
- 自動コミットが `.agent/` 以外の変更を巻き込みそうになって中止したあと、ステージング状態が
  変わったまま残っていた。人間が次にコミットしたときに意図しないファイルが混ざる問題を修正した。
- リベースなど、マージ以外の Git 操作が中断したまま残っている作業ツリーが、再開の対象として
  選ばれないことがある不具合を修正した。
- 引数を誤ったときのエラーメッセージが、実際のコマンド名(`ccloop`)と受け付けるオプションを
  正しく案内するようになった。
- 自動マージが衝突を自動で解消したときのログが、実際に解消した対象と食い違っていた。決定の一覧
  だけを解消した場合でも「タスクファイルはブランチ側を採用」と表示していたのを、実際に解消した
  内容を表示するようにした。
- 探索セッションが起動直後に異常終了したとき、GOAL の変更や確認事項への回答が読まれないまま
  「確認済み」として扱われ、以降の実行が何もせずに終わり続けることがある不具合を修正した。
- `ccloop add --deps` に存在しないタスク ID を指定した場合、黙って登録せずエラーにするようにした。
- 同じリポジトリで `ccloop run` を二重に起動すると、先に動いているループの実行状態が壊れる問題を
  修正した(二重起動は拒否される。`--force` で従来どおり起動できる)。
- `ccloop status` が、ループ本体が止まっているのに実行中のタスクを動いているかのように表示して
  いたのを直した。止まっているときは記録が残っているだけだと分かる表示になる。
- `ccloop status` の稼働状態に「セッション数」という表記が 2 か所あり、値が違っても見分けが
  つかなかった。これまでに起動したセッション数と、費用の集計対象になった終了済みセッション数を
  区別して表示するようにした。
- 探索セッションが持ち時間切れで終わったとき、ログが「異常終了」としか表示されず、本当にクラッシュ
  したのか時間切れなのか見分けられなかった。時間切れのときはタイムアウトと分かる表示にした。
- 記録ファイル(タスク・確認事項など)の先頭に BOM が付いていたり改行が CRLF だったりすると、
  frontmatter が読めず、タスクが一覧から消えたり確認事項のタイトル・重要度が失われたりする
  不具合を修正した。
- 片付け(archive への移動)で移動先に同じ名前の記録が既にある場合、上書きせずに移動を見送り、
  警告を表示するようにした。
- 片付けが失敗して同じタスクの記録が 2 か所に残ったとき、`ccloop status` の進捗表示で
  そのタスクが二重に数えられてしまう問題を直した。
- `.agent/config.json` の項目が壊れているとき、どの項目がどうおかしいかを示して止まるように
  なった(以前は原因の分からないエラーで落ちていた)。
- `.agent/` の配置先(`tasks/` など)と同名のファイルが既にあると、内部エラーで中途半端な状態
  のまま落ちていたのを修正した。どのパスが問題かを示して安全に中断するようになった
  (既存のパスは消さない)。あわせて `ccloop doctor` が、`.agent/` の不足ファイルや
  `.gitignore` の記載漏れも検出するようになった。
- 中途半端な `.agent/` を「配置済み」と誤認しなくなった。ファイルが欠けている場合は
  `ccloop init` の再実行で不足分だけが補われる。`run` などのコマンドも `.agent/` が
  不完全なら配置を促すようになったため、以前は素通りしていた状態で確認を求められることがある。
- main へのマージ衝突によるリトライが、タスク本来のやり直し回数を消費しなくなった。マージ衝突は
  タスクの中身の失敗ではないため、衝突用の別枠(`maxConflictRetries`、既定 5 回)で数えるように
  なった。実装自体は完成していたのに、マージ衝突が重なっただけでタスクが failed になる事故が
  無くなる。
- ターン数の上限で打ち切られたセッションが失敗として数えられず、同じタスクが延々と
  再実行され続ける問題を修正した。
- 終わったタスクや確認事項の archive への片付けが、実行中のセッションが途切れない状況では
  一度も進まないことがある不具合を修正した。次の作業を探すセッションの起動前にも片付けが
  行われるようになった。
- 利用上限に当たったままセッションが時間切れで打ち切られると、利用上限として扱われず
  やり直し回数を消費していた問題を修正した。時間切れであっても待機してから再開するようになった。
- タスクの依存に実在しないタスク ID を書いてしまった場合に `ccloop status` の要対応に出るように
  なった。あわせて `ccloop list` でもその依存行が淡色にならず目立つようになった。これまでは
  依存が黙って無視され、意図した順序が崩れていることに気づけなかった。
- タスクの後始末(main への統合と作業場所の片付け)の途中で `ccloop run` が強制終了されても、
  そのタスクが未着手のまま記録なく元に戻ることがなくなった。次回の起動時に中断が検出され、
  再実行時に成果が main へ入っていないか確認するよう案内される。
- main との衝突を解消しきれないまま持ち時間が尽きたタスクが、衝突ではなくタイムアウトを理由に
  記録され、次の試行にタスクを小さく割るよう促してしまう不具合を修正した。衝突が残っている場合は
  マージ衝突として記録され、次の試行には衝突の解消を促すようになった。
- マージ直後に git の内部状態が一時的に信用できなくなることがあり、その際に自動マージの
  巻き戻しに失敗してループ全体が停止する不具合を修正した。少し待ってから巻き戻しを
  再試行するようになった。
- タスクファイルの衝突を自動解消するとき、破棄した main 側の変更内容をマージコミットの
  メッセージに残すようにした(人間が手で編集した内容が黙って消えなくなった)。
- 起動時の復旧処理が、中断していたタスクのブランチや作業場所を片付けた直後に `ccloop run` が
  強制終了されても、そのタスクが未着手のまま記録なく元に戻ることがなくなった。次回の起動時に
  中断が検出され、記録が再生される。

## 0.4.1 — 2026-08-29

- 起動時の孤児ブランチの回収が、未コミットの変更があると止まってしまい復旧が遅れる不具合を修正した。
- `.agent/` の自動コミットのタイミングを見直した。頻度を抑えつつ、セッション起動前・マージ直前・
  終了時には確実にコミットされる。
- フェーズの続行確認は、複数のトピックを並行して進めてよいことを明確にした。

## 0.4.0 — 2026-08-29

- フェーズゲートを導入した。「コア機能が試せる → 全機能が試せる → 本番リリースできる → 改善」の
  節目を越える前に、続行してよいかを確認事項として尋ねるようになった。途中で方針を変えたり
  止めたりする機会が挟まる。
- `ccloop status` の BLOCK 表示から、フェーズの進行が止まっていることも分かるようになった。
- `npm run release` を追加した。バージョン整合の確認・検証・タグ付け・push までを 1 コマンドで
  行う(`--dry-run` で変更操作の手前まで試せる)。

## 0.3.1 — 2026-08-23

- レートリミット解除後に ccloop を再起動しなくても、自動で起動抑制が解けるようになった。
  解除直後にセッションがすぐ終了しても、異常終了として扱われなくなっている。
- 自動実行時の権限(許可・拒否)の設定を実際の動作に合わせて整理し、安全側に寄せた。
- 探索セッションが並列実行の枠を超えて起動しないようにした。

## 0.3.0 — 2026-08-23

- `ccloop --help` を充実させ、全サブコマンドの使い方・オプション・既定値を確認できるようにした。
- `ccloop status` と `ccloop list` に `--json` を追加した。
- Ctrl+C による停止を 3 段階(クリーン停止 → 緊急停止 → 即終了)に整理した。
- クリーン停止のとき、マージ衝突が残っていれば停止前に解消セッションを起動するようにした
  (レートリミット中や異常終了が続いているときは、無駄に起動しない)。

## 0.2.0 — 2026-08-22

- タスクや確認事項のファイル ID を連番から「日時 + 内容を表す英語」の形式に変えた。
  ファイル名だけで中身の見当がつく。あわせて `ccloop add` に `--slug` を追加した。
- 並列実行時に ID の採番が衝突する問題が解消された(新しい形式では番号の振り直しが起きない)。
- ccloop 0.1.0 を devcontainer feature として公開し、実際に組み込んで使える形にした。

## 0.1.0 — 2026-08-22

- 初回公開。GOAL を与えると、探索 → タスク分解 → 実装 → 検証 → 人間レビューのループが
  自律的に回る CLI。
- devcontainer feature として配布し、Codespaces などにそのまま組み込める。
- `ccloop init` でリポジトリへの初期セットアップ、`ccloop doctor` で環境の診断ができる。
- 継続監視での実行を組み込みの動作にした。停止は Ctrl+C で行う。
