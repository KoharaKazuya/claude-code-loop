---
title: "CHANGELOG 未リリース節の記載漏れ 3 件を補う"
status: ready
priority: 3
dependencies: []
conflicts: [T-20260830-1440-doc-followup-changelog-readme]
retries: 0
createdAt: 2026-08-30T14:52:00.000Z
---

所属フェーズ: 4(思いつく改善すべて)。「入れた変更に対する記載漏れの解消」であり、
`D-20260830-0303-phase4-consent-granularity` の線引きに従い事前の確認(Human Review)は不要な範囲。

## 目的

利用者から見た挙動が変わった変更 3 件が `CHANGELOG.md` の「## 未リリース」節に載っていない。
このままリリースすると、利用者向けリリースノートから 3 件が欠けたまま公開される。

## 確認済みの事実(この調査で裏を取った内容)

- `CHANGELOG.md` は `3a2a0e6`(2026-08-30 03:52:57 +0000)で新設され、そこまでの分はまとめて記載済み。
  下記 3 件はいずれも `3a2a0e6` の子孫であり(`git merge-base --is-ancestor` で確認)、
  「新設時にまとめて書かれた」対象ではない。各コミット自身も `CHANGELOG.md` を変更していない。
- 未リリース節(9〜170 行)を関連語(優先度 / 依存 / PID / 生存 / 確認待ち / スヌーズ / note ほか)で
  検索し、下記 3 件に対応する行が無いことを確認した。既存の類似行は別のコミットに対応するもの:
  - 96 行目「ループ本体が止まっているのに実行中のタスクを…」は `933af26` の分であり、
    PID 使い回し(`66803bc`)とは別の不具合。
  - 18 行目「生存記録が読めないときも『不明』と表示」は `b085214` の分。
  - 101 行目付近の `--deps` 存在チェックは `dee1434` の分であり、`57c6506` の trim 漏れとは別。

## 追記するもの(3 件)

利用者の言葉で 1 行ずつ「## 未リリース」節の適切な小見出し(追加 / 修正)へ足す。
内部の関数名・ファイルパス・タスク ID は書かない。

1. `57c6506 fix(cli): ccloop add のオプション入力を検証する` → **修正**
   `ccloop add --priority` に数値として解釈できない値を渡すと、そのまま壊れた値がタスクファイルへ
   書き込まれていたのをエラーにするようにした。あわせて `--deps` を `"T-a, T-b"` のように空白付きで
   書くと空白込みの存在しない ID として登録され、指定した依存関係が黙って無効になっていた問題も直した。

2. `33850e9 feat(status): 確認待ち・待機中の行に文脈を添える` → **追加**
   `ccloop status` の未回答の確認事項の行に、何を聞かれているかの要約が添えられるようになった。
   待機中(スヌーズ中)のタスクの行にも、なぜ待っているかのメモが表示される。`--json` にも含まれる。

3. `66803bc fix(status): PID 使い回しでループが生きていると誤表示されるのを防ぐ` → **修正**
   `ccloop run` が異常終了した直後に、別のプロセスがたまたま同じプロセス番号を取ると
   `ccloop status` がループを「動いています」と誤表示することがあったのを直した。

## 完了条件

- 上記 3 件が「## 未リリース」節に、既存の行と同じ書きぶり(利用者の言葉・1〜3 行)で載っている。
- バージョン見出しを手で作らない(繰り上げは `npm run release` が行う)。
- `npm test` / lint / typecheck が通る(文書のみの変更でも実行して確認する)。
- 変更を日本語のコミットメッセージでコミットする(`docs(changelog): …`)。

## 補足

追随漏れの機械的な検出を作らない判断は `D-20260830-1443-changelog-followup-not-mechanized` に
記録済み。この 3 件は探索セッションの突き合わせで拾えたので、その判断は維持でよい
(このタスクで仕組み化を検討しないこと)。
