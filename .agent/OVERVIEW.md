---
updatedAt: 2026-08-30T06:21:28.294Z
completed: 44
total: 55
---

# OVERVIEW(GOAL に対する現在地)

GOAL の「現在の目標」は 2 つとも、当初挙がっていた作業を消化し終えている。

1. decisions アーカイブの人間承認化と `ccloop status` への未承認決定の表示 — 完了。
2. 人間の確認・介入の導線を磨く — 変更履歴、手元の ccloop を入れ替える手順の文書化、
   `ccloop status` の文脈表示、ループの生存表示、`ccloop retry` までが完了。

以後の前進は、**フェーズ 4 の確認トピックを人間に投げて枠を回し続けること**と、
**確認不要で拾える修理を掘り当てること**の両輪で決まる。後者の収穫は依然として安定している
(未調査の系統を選べば毎回 2〜4 件)。

**今回いちばん重要な発見は、成果が静かに失われる経路が 2 つ見つかったこと**である
(下記「取り残された成果」と「大きな差分の消失」)。どちらも「ループは動いているのに
結果だけが消える」種類で、人間が気づきにくい。

## 取り残された成果(最優先)

`T-20260830-0537-max-turns-not-failure`(p2)は **3 回の試行すべてが main へのマージ衝突で
失敗**して `failed` になった。作業自体は 1 回目で完成しており、実装・テスト・文書・判断記録が
ブランチ `agent/conflict/T-20260830-0537-max-turns-not-failure-20260830T061327Z` に
コミット済みのまま残っている。main には一切入っていない(確認済み)。

- `T-20260830-0621-rescue-max-turns-work`(p1)— その成果を main へ取り込む。
  ブランチ操作は権限で使えないのでパッチ経由で当てる手順をタスクに書いた。
- `T-20260830-0621-conflict-retry-always-reconflicts`(p2)— **なぜ 3 回とも同じ衝突を
  繰り返したか**の再発防止。失敗を記録する行為(タスクファイル末尾への試行履歴の追記)が
  次の衝突を生んでいる疑いが濃い。やり直し回数がタスクの難しさではなくマージ機構の都合で
  消費されている。仮説の裏取りから始める形で書いた。

## いま動いているもの

- 完了・マージ済み(直近): 権限の追記がループ再起動まで効かない件、`config.json` の項目検証、
  強制終了タイマーの解除、探索セッションの起動失敗で `ccloop run` ごと落ちる件。
- 実装中(他セッション、いずれも修理なので確認は取っていない):
  - `T-20260830-0602-status-shows-dead-session-running`(p2)
  - `T-20260830-0602-invalid-task-status-silently-dropped`(p3)
  - `T-20260830-0602-init-path-type-collision`(p4)
- 着手待ち(今回登録):
  - `T-20260830-0621-rescue-max-turns-work`(p1)/ `-conflict-retry-always-reconflicts`(p2)
    — 上記。
  - `T-20260830-0621-large-diff-salvage-lost`(p2)— **大きな未コミット差分が退避に失敗した
    まま作業場所ごと消される**。退避は `git diff --binary` の出力を既定 1MB の受け皿で
    受けており、超えると例外になる。例外は握りつぶされ警告 1 行だけ残り、その直後に
    **無条件で**削除が走る。コミットしていないので git 側にも残らず復元不能。実機で再現確認済み。
    3 経路すべて同じ順序。
  - `T-20260830-0621-doctor-auth-check`(p3)/ `-timeout-retry-split-hint`(p3)/
    `-housekeeping-on-explore`(p3)/ `-background-run-wording`(p4)— いずれも人間の回答から
    起こしたもの(下記「フェーズ」参照)。
- 確認待ち(BLOCK 4 件、枠は満杯): 下記「フェーズ」参照。

main の健全性は 2026-08-30T05:25Z 時点でテスト 960 件すべて成功を確認済み。

## フェーズ

- 現在フェーズ: **4(思いつく改善すべて)**。フェーズ 1〜3 の遡及確認とフェーズ 4 の運用可否は
  `HR-20260830-0236-phase-gate-applicability` で回答済み・closed。
- 確認の線引きは `D-20260830-0303-phase4-consent-granularity` に記録済み。
- **今回、回答待ちだった 4 件すべてに回答が付いた**。取り込み済み(すべて closed):
  - 裏で動かすループの止め方 → **やらない**。「起動した画面を開いたままにする」使い方が想定であり、
    裏で動かす推奨の記述を直すことになった(`D-20260830-0621-foreground-run-is-the-supported-usage`、
    `T-20260830-0621-background-run-wording`)。
  - doctor にログイン確認 → **やってよい**(`T-20260830-0621-doctor-auth-check`)。
  - 時間切れが続く大きなタスク → **案 2 のみ**。持ち時間は伸ばさず、分割を促す申し送りだけ直す
    (`T-20260830-0621-timeout-retry-split-hint`)。
  - 忙しい間の片付け → **探索セッションの実行時に行う**。時間間隔で細かく走らせる案は不採用で、
    粒度を細かくしないこと(`T-20260830-0621-housekeeping-on-explore`)。
- 見送りになったトピック(再提案しないこと):
  - タスクに「最後に動いた時刻」を記録して status に出す(`D-20260830-0429-task-last-updated-declined`)
  - 手元の ccloop を入れ替える専用サブコマンド → 文書化のみと決着
  - **ループのプロセス番号を記録・表示する / 停止の導線を増やす**
    (`D-20260830-0621-foreground-run-is-the-supported-usage`)
  - **1 セッションの持ち時間を再試行のたびに伸ばす**(上記の回答で明確に不採用)
- 回答待ちの確認トピック(4 件。枠は満杯。在庫から繰り上げた):
  - `HR-20260830-0621-topic-fatal-stop-invisible` — ループの異常停止を status に残すか
  - `HR-20260830-0621-topic-status-grows-slow` — 記録が増えると状況表示が重くなる件
  - `HR-20260830-0621-topic-ratelimit-not-shared` — 複数リポジトリで利用上限の待機を共有するか
  - `HR-20260830-0621-topic-dependency-cycle` — 依存が輪になったときの警告(優先度低)

## 確認トピックの在庫

**在庫は空になった**(4 件すべてを HR に出した)。次に枠が空いたときは、新しく掘り当てた
「仕様の選択」に当たる論点をここに積むこと。現時点で在庫に残っているのは 1 件だけ:

1. (優先度低)**走行中タスクの記録ファイルを探索セッションが編集しない保証が、プロンプトの
   指示だけである件**。コード側の強制は無い(`lib/supervisor.ts` が一覧を注入するのみ。
   `lib/hooks/stop-check.ts` は探索セッションでは何も検査せず即終了する)。違反しても同じ行なら
   3-way マージが衝突として検出して止まるため、実害は「別々の行の編集が静かに混ざる」程度。
   強制を足すかは設計判断。

## 突き合わせ済みで食い違いが無いことを確認した範囲

同じ調査を繰り返さないための記録。

- `lib/prompt/PROMPT.md` と実装、`README.md` / `docs/` / `lib/help.ts` と実装、初回導入の経路:
  いずれも突き合わせ済みで食い違い無し。
- 掘り終えた系統: 利用者から見た CLI の体験 / ループの運転そのものの弱点 / 初回導入の体験 /
  並列実行と状態ディレクトリ / 長時間の連続稼働 / 複数リポジトリの併用 / 費用・所要時間 /
  記録ファイルの読み書きの頑健性 / 失敗時の診断体験 / テスト自体の質 /
  探索セッションとタスクセッションの並走 / Claude Code プロセスの起動と結果の解釈 /
  セッションに渡す設定の組み立て / `ccloop status`・`watch` の表示ロジック /
  `.agent/` 雛形と実リポジトリの往復 / **git 操作そのものの異常系**(今回)。
- **git 操作の異常系**(今回調査): 上記 `large-diff-salvage-lost` が該当。以下は問題無しと
  確認済みなので再調査しなくてよい —— `lib/merge.ts` の自動マージ(進行中の git 操作を横取り
  しない事前ガード、`merge --abort` 失敗時の分離、racy git 対策の `update-index --refresh`)、
  `gitOperationInProgress` / `mergeInProgress` が linked worktree で `.git` がファイルになる
  ケースを吸収していること、`removeWorktree` の削除失敗時フォールバック、
  `commitAgentDir` の二段階チェック(人間の並行作業を壊さない)、起動時復旧が `MERGE_HEAD` の
  SHA 突合で人間のマージに触れないこと。
  未検証のまま残った枝葉(実害は薄いと判断): submodule を含むリポジトリでの worktree 作成、
  shallow clone での `merge-base`、`.gitattributes` / `core.autocrlf` の正規化が退避パッチの
  復元に与える影響、`index.lock` 残存時の挙動。いずれも通常の git のエラー終了に帰着し、
  既存の try/catch で警告ログに落ちると見られる。

## 次にやると完了に近づくこと

**`T-20260830-0621-rescue-max-turns-work`(p1)を最優先で入れること。** 完成済みの成果が
衝突ブランチと退避パッチにしか無く、片付け処理がそれらを消せば失われる。時間が経つほど
危険が増える唯一のタスクである。

その次が p2 の 3 件。いずれも「静かに失われる・嘘をつく」系統で、人間の介入判断を直接狂わせる:
`large-diff-salvage-lost`(成果が消える)、`conflict-retry-always-reconflicts`(やり直し回数が
中身と無関係に尽きる)、`status-shows-dead-session-running`(状況表示が嘘をつく)。

次の探索セッションへの申し送り:

- 「突き合わせ済み」の範囲は再調査しなくてよい。まだ当たっていない系統は次の 2 つ:
  - **エスカレーション・リトライ方針の実効性**(今回着手したが完了していない。
    `retries` の増減・モデル昇格・crash-backoff・レートリミット待機・スヌーズが
    組み合わさったときに意図通り働くか)。**今回見つかった衝突の件がまさにこの系統の
    実例なので、有望である。**
  - プロンプト注入の内容がセッションの挙動に与える影響(長さ・順序・重複)。機械的に
    検証しづらく、修理として拾える見込みは薄い。最後に回してよい。
- 確認枠は再び満杯(BLOCK 4 件)。新しい確認トピックは出せないので、掘り当てた論点は
  上記「在庫」に積むこと。
- `.agent/decisions/index.md` に直近の判断が載っていないのは片付けが走っていないためで、
  `T-20260830-0621-housekeeping-on-explore` が対象。手で埋めないこと。
