---
updatedAt: 2026-08-30T11:51:08.359Z
completed: 75
total: 79
---

# OVERVIEW(GOAL に対する現在地)

GOAL の「現在の目標」に挙がっていた作業(decisions アーカイブの人間承認化、`ccloop status` への
未承認決定の表示、人間の確認・介入の導線)はいずれも消化済み。以後の前進は、
**フェーズ 4 の確認トピックを人間に投げて枠を回し続けること**と、
**確認不要で拾える修理を掘り当てること**の両輪で決まる。

## 「衝突による成果の取り残し」は峠を越えた

構造(`lib/supervisor.ts` が 5800 行超の単一ホットスポットで、並列セッションが全員そこを編集する)は
変わっていないが、取り残しを生んでいた穴は塞がった。後始末の中断で成果が痕跡なく消える経路
(`-0829` / `-0934`)は両方実装済み、退避ブランチの取り込み状態は status に出る。応急処置の
直列化の鎖(`D-20260830-1053-serialize-supervisor-hotspot-tasks`)は構成タスクが全て完了し
**現在ゼロ**。恒久策(同じファイルを取り合うタスクを同時に選ばない)は
`HR-20260830-1053-topic-conflict-prone-parallelism`(BLOCK)で確認中。

## 探索の収穫が薄くなってきた(いま最も重要な事実)

実物観察(`npm test` 1197 件全通過 / lint・typecheck 警告ゼロ / README・docs・`lib/help.ts`・
`lib/config.ts` と実装の突き合わせ / `lib/` の TODO・FIXME 掃き出し / CHANGELOG 未リリース節と
直近 30 コミットの照合)を回しても**修理ネタがゼロ**の回が 2 回続いた(11:47 / 11:51)。
掘り尽くしに近づいている。この 2 回目の空振りは
`HR-20260830-1143-topic-idle-explore-backoff`(空振りが続いたら次の探索までの間隔を自動で
伸ばしてよいか)の根拠を 1 件強めた。

## いま動いているもの

ready 3 件で 3 件とも実行中、待機列は空。走行中タスクのファイルは編集しない方針のため
priority・dependencies は変更していない(鎖が解けており張り直す必要も無かった)。

- 実行中: `-1132-ccloop-in-worktree-targets-main-agent-dir`(p2、衝突解消の継続)/
  `-1143-docs-idle-exit-behavior`(p4)/ `-0825-archived-tasks-read-cap`(p5)
- 今回登録した新規タスク: **なし**(修理ネタが無く、改善トピックはフェーズ 4 の同意待ち)。

## 人間の手が要る残務

- `failed` の `-0829-finish-crash-leaves-no-trace` は成果が main に取り込み済みで再試行不要。
  `ccloop abandon` で要対応欄から消せる(コマンドは実装済み)。この「別タスクが肩代わりした
  失敗タスクが人手待ちで残る」パターンは今日 2 回目なので、`HR-20260830-1151-topic-superseded-failed-task-cleanup`
  で仕組み化の可否を確認中。
- 退避ブランチ `agent/conflict/T-20260830-0829-...-20260830T101839Z` は**削除して構わない**。
  今回中身を確認し、実装・テスト・判断記録・CHANGELOG のすべてが main のコミット `7683a0d`
  に入っていることを確認済み(ブランチ側にしか無いのはタスクファイルの status/note 更新だけ)。
  `ccloop status` が「未取り込み」と出し続けるのは、取り込み時にコミットが当て直されて
  `git rev-list HEAD..<branch>` が 0 にならないため。`parkedBranchMergedIntoHead`
  (`lib/supervisor.ts:1245-1270`)が patch-id 比較を避けて安全側に倒す設計どおりで、
  **不具合ではないので修理タスクにしない。**
- 未承認の決定が 15 件。`.agent/decisions/index.md` のチェックで書庫へ移る。

## フェーズ

- 現在フェーズ: **4(思いつく改善すべて)**。フェーズ 1〜3 の遡及確認は `HR-20260830-0236` で回答済み。
- 確認の線引きは `D-20260830-0303-phase4-consent-granularity` に記録済み。
- 回答待ち(4 件。**枠は 4 件までなので満杯**。回答が返るまで新しいトピックは出さない):
  - `-1053-topic-conflict-prone-parallelism` — 同じファイルを取り合う並列実行を減らすか
  - `-1057-topic-denied-operation-audit` — 禁止した操作を試みた事実を残すか
  - `-1143-topic-idle-explore-backoff` — 空振りが続いたら作業探しの間隔を伸ばすか
  - `-1151-topic-superseded-failed-task-cleanup` — 肩代わりされた失敗タスクを人手を待たず片付けるか
- 見送りになったトピック(再提案しないこと): ループの異常停止を status に残す /
  利用上限の待機をリポジトリ間で共有する / タスクに「最後に動いた時刻」を記録して status に出す /
  管理から外れた作業場所の検出 / 実行中タスクの記録を仕組みで守る /
  手元の ccloop を入れ替える専用サブコマンド / ループのプロセス番号を記録・表示する /
  1 セッションの持ち時間を再試行のたびに伸ばす。
- 確認トピックの在庫は**空**(今回掘り当てた 1 件はその場で提出し、枠が満杯になった)。

## 突き合わせ済みで食い違いが無いことを確認した範囲(同じ調査を繰り返さないための記録)

- permissions.deny の二重管理(`lib/settings.template.json` ⇔ `lib/prompt/PROMPT.md`)は
  `lib/deny-consistency.test.ts` が機械的に検証している。**手で突き合わせ直す必要はない**
  (`npm test` が通っていれば一致している)。
- `README.md` / `docs/` / `lib/help.ts` と実装、初回導入の経路、`lib/settings.template.json` と
  `.agent/claude-settings.json` の合成、`.agent/config.json` の各キーと既定値、
  `CHANGELOG.md` の未リリース節と `git log`、`lib/templates/` と `docs/compatibility.md`、
  `lib/` の TODO/FIXME: いずれも食い違い無し(11:51 に再確認)。残る欠落は
  **ループ自動終了の説明**だけで `-1143` が補っている。
- CLI の異常系(`--interval` の 0 / 負値 / 非数値、存在しない `--repo`、未知サブコマンド、
  存在しないタスク ID への `retry`)はいずれも適切なメッセージを返す。
- **worktree の使い回しは意図された挙動**。`createWorktree` はパスが既存なら再利用する。退避に
  失敗した worktree がブランチごと残り次の試行で再利用されることは
  `D-20260830-0752-salvage-failure-keeps-worktree` に記録済み。クラッシュ由来の残骸と衝突解消の
  継続をコードは区別しないが、「管理から外れた作業場所の検出」は見送り済みトピックなので蒸し返さない。
- **やることが尽きたときの挙動**: `planLoopStep`(`lib/scheduler.ts`)が `idle-exit` を返して
  ループ自体が終了する。空振り探索は既定 1 時間のクールダウンを課すが、その記憶はプロセス内
  メモリのみで再起動すると消え、間隔も伸びない(→ `HR-...-1143` の論点)。
- 掘り終えた系統: 利用者から見た CLI の体験 / ループの運転そのものの弱点 / 初回導入の体験 /
  並列実行と状態ディレクトリ / 長時間の連続稼働 / 複数リポジトリの併用 / 費用・所要時間 /
  記録ファイルの読み書きの頑健性 / 失敗時の診断体験 / テスト自体の質 /
  探索セッションとタスクセッションの並走 / Claude Code プロセスの起動と結果の解釈 /
  セッションに渡す設定の組み立て / `ccloop status`・`watch` の表示ロジック /
  `.agent/` 雛形と実リポジトリの往復 / git 操作そのものの異常系 /
  エスカレーション・リトライ方針の実効性 / 作業場所とタスクの対応が崩れたときの回復 /
  `.agent/` の記録が人手で編集されたときの扱い / 多段処理の中断耐性 / 失敗回数の計上 /
  スケジューラの待機と終了の判断。
- 未検証のまま残った枝葉(実害は薄いと判断): submodule を含むリポジトリでの worktree 作成、
  shallow clone での `merge-base`、`.gitattributes` / `core.autocrlf` が退避パッチの復元に
  与える影響、`index.lock` 残存時の挙動、dirty な worktree を再利用したときの実挙動のテスト。

## 次にやると完了に近づくこと

1. **回答待ち 4 件のいずれかに回答が返ること。** 枠が満杯なので、回答が返るまで
   フェーズ 4 の新しい作業は増やせない。特に `-1053-topic-conflict-prone-parallelism`
   (並列実行の衝突を人力運用で抑え続けるか仕組みにするか)。
2. 実行中 3 件の消化。待機列は空なので、これらが終わるとループは idle-exit する。
3. `ccloop abandon` と退避ブランチの削除、決定 15 件の承認(人間の手)。

次の探索セッションへの申し送り:

- **確認トピックの枠は満杯(4/4)。新しいトピックを提出しないこと。** 回答が返って枠が
  空くまでは、拾えるのは「確認不要の修理」だけである。
- **ネタが枯れてきている。** 無理にひねり出すより「収穫ゼロ」を正直に記録するほうがよい
  (その事実自体が `-1143` の根拠になっている)。当たり続けていた切り口「リポジトリの実物を
  見る」も直近 2 回はゼロ。残るのは未検証の枝葉(上記)か、まだ掘っていない「プロンプト注入の
  内容がセッションの挙動に与える影響(長さ・順序・重複)」だが、後者は機械的検証がしづらい。
- **要対応欄に出ている残務を「修理ネタ」と取り違えないこと。** 退避ブランチの
  「未取り込み」表示は設計どおりであり(上記)、失敗タスクが残るのは人間の操作待ちである。
- **直列化の鎖は現在ゼロ。** `lib/supervisor.ts` を触るタスクを 2 件以上同時に ready にするなら、
  `dependencies` で直列化してから終えること(張り忘れると衝突が再発する)。
- 走行中のタスク(ブランチ `agent/T-*` が存在するもの)のファイルは編集しない。今回も 3 件が
  走行中だったため priority・dependencies には触れていない。
- 調査で `Bash(git worktree*)` とブランチ操作が権限で使えないため、作業場所まわりは実機再現が
  できない。コード読解で当たりを付け、再現は着手するタスクセッション側に委ねる形になる。
