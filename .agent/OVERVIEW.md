---
updatedAt: 2026-08-30T13:37:05.423Z
completed: 78
total: 82
---

# OVERVIEW(GOAL に対する現在地)

GOAL の「現在の目標」に挙がっていた作業(decisions アーカイブの人間承認化、`ccloop status` への
未承認決定の表示、人間の確認・介入の導線)はいずれも消化済み。現在フェーズは 4(思いつく改善すべて)で、
前進は **フェーズ 4 の確認トピックに回答をもらうこと**と、**確認不要で拾える修理**の両輪で決まる。

## 今回、確認トピック 4 件すべてに回答が返った(枠は 4/4 → 0/4)

- **やる**: `-1053-topic-conflict-prone-parallelism`(同じファイルを取り合う並列実行を減らす)。
  採用された方式は人間の指定どおり **「探索セッションが競合しそうなタスクを判断し、依存と同じように
  タスクへ競合のメタデータ(`conflicts`)を持たせ、タスク選択時に同時実行しない」**。
  仕組み側と指示側の 2 タスクに分けて登録済み(下記)。巨大ファイルの分割は選ばれなかったので蒸し返さない。
- **やらない**(再提案しないこと): `-1057-topic-denied-operation-audit`(禁止操作の監査記録)/
  `-1143-topic-idle-explore-backoff`(空振り時の探索間隔の自動延長)/
  `-1151-topic-superseded-failed-task-cleanup`(肩代わりされた失敗タスクの自動片付け)。

`-1057` の暫定判断にあった「回答が無ければ共通ルールの記述を実態に合わせて直す」は **不要だった**。
今回突き合わせた結果、実装(`recordPermissionDenials` / `partitionDeniedByRules`)・
`lib/prompt/PROMPT.md`(絶対ルール 6、Bash 権限制約節、Human Review 節)・`docs/architecture.md`・
`README.md` はすべて「deny 一致の拒否は記録しない、classifier 判定の拒否だけ記録・要約表示する」で
一貫していた。**同じ突き合わせを繰り返さないこと。**

回答から読み取れる人間の好み: **ループが自分で判断して人間の目に触れずに片付ける方向は採らない。
一方、事故(衝突による成果の取り残し)を構造的に防ぐ仕組みは入れてよい。** 次のトピックを出すときの目安になる。

## いま登録されているタスク(ready 4 件)

衝突回避のため直列化してある(`-changelog…` → `-task-conflict-metadata…` → `-explore-assigns…`)。
前 2 者は `CHANGELOG.md` の同じ節を、後 2 者は `lib/supervisor.ts` を取り合うため。

- `-1334-changelog-doctor-agentroot-fix`(p2)— `3d97f30` の doctor 修正が変更履歴に載っていない。
  `CHANGELOG.md` のみの小さな修理。
- `-1334-task-conflict-metadata-scheduling`(p2)— `conflicts` フィールドとタスク選択の実装。
- `-1334-explore-assigns-conflict-metadata`(p3)— 探索セッションが `conflicts` を付ける指示を
  `lib/prompt/PROMPT.md` と探索プロンプトへ定着させる。これが無いとフィールドは空のままで何も抑止しない。
- `-0829-finish-crash-leaves-no-trace`(p3)— **中身は既に main にある**
  (`13c4916` ほか、`phase: "finishing"` による起動時復旧が `lib/supervisor.ts:1842-1870` に実装済み)。
  `note` にもその旨が書いてある。着手したセッションは実装をやり直さず、成果が入っていることを
  確認して `completed` にすればよい。`-1151` の回答により、この片付けをループ側で自動化する案は不採用。

## 「衝突による成果の取り残し」の現在地

後始末の中断で成果が痕跡なく消える経路(`-0829` / `-0934`)は両方実装済み、退避ブランチの取り込み状態も
status に出る。応急処置だった手張りの直列化(`D-20260830-1053-serialize-supervisor-hotspot-tasks`)は
恒久策(`conflicts`)へ移行する。構造(`lib/supervisor.ts` が 5800 行超の単一ホットスポット)は変わらない。

## 探索の収穫は薄い(3 回連続でほぼゼロ)

実物観察(`npm test` 1215 件全通過 / lint・typecheck 警告ゼロ / README・docs・`lib/help.ts`・
`lib/config.ts` と実装の突き合わせ / `lib/` の TODO・FIXME / CHANGELOG と直近 30 コミットの照合)を
回して、今回拾えたのは **CHANGELOG の記録漏れ 1 件だけ**。掘り尽くしに近い。`-1143` が不採用に
なったので、空振りしても探索は 1 時間おきに走り続ける。**無理にひねり出さず「収穫ゼロ」を
正直に記録するほうがよい。**

## 人間の手が要る残務

- 未承認の決定が 15 件。`.agent/decisions/index.md` のチェックで書庫へ移る。
- 退避ブランチ `agent/conflict/T-20260830-0829-...-20260830T101839Z` は**削除して構わない**。
  中身はすべて main のコミット `7683a0d` に入っていることを確認済み(ブランチ側にしか無いのは
  タスクファイルの status/note 更新だけ)。status が「未取り込み」と出し続けるのは
  `parkedBranchMergedIntoHead`(`lib/supervisor.ts:1245-1270`)が patch-id 比較を避けて安全側に倒す
  設計どおりで、**不具合ではないので修理タスクにしない。**

## フェーズ

- 現在フェーズ: **4(思いつく改善すべて)**。フェーズ 1〜3 の遡及確認は `HR-20260830-0236` で回答済み。
- 確認の線引きは `D-20260830-0303-phase4-consent-granularity` に記録済み。
- 回答待ち: **0 件(枠は 4 件まで空いている)**。ただし**トピックの在庫も空**で、今回は新しく
  提出していない(修理以外に提案できるネタが無かった)。
- 見送りになったトピック(再提案しないこと): 禁止操作の監査記録 / 空振り時の探索間隔の自動延長 /
  肩代わりされた失敗タスクの自動片付け / 巨大ファイルの分割 / ループの異常停止を status に残す /
  利用上限の待機をリポジトリ間で共有する / タスクに「最後に動いた時刻」を記録して status に出す /
  管理から外れた作業場所の検出 / 実行中タスクの記録を仕組みで守る / 手元の ccloop を入れ替える
  専用サブコマンド / ループのプロセス番号を記録・表示する / 1 セッションの持ち時間を再試行のたびに伸ばす。

## 突き合わせ済みで食い違いが無いことを確認した範囲(同じ調査を繰り返さないための記録)

- permission 拒否の記録(実装 ⇔ PROMPT.md ⇔ docs ⇔ README): 一致。上記のとおり 13:34 に確認。
- permissions.deny の二重管理(`lib/settings.template.json` ⇔ `lib/prompt/PROMPT.md`)は
  `lib/deny-consistency.test.ts` が機械検証している。**手で突き合わせ直す必要はない。**
- `README.md` / `docs/` / `lib/help.ts` と実装、`.agent/config.json` の各キーと既定値、
  `lib/templates/` と `docs/compatibility.md`、`lib/` の TODO/FIXME: いずれも食い違い無し(13:34 に再確認)。
- CLI の異常系(`--interval` の 0 / 負値 / 非数値、存在しない `--repo`、未知サブコマンド、
  存在しないタスク ID への `retry`)はいずれも適切なメッセージを返す。
- **worktree の使い回しは意図された挙動**(`D-20260830-0752-salvage-failure-keeps-worktree`)。
- **やることが尽きたときの挙動**: `planLoopStep`(`lib/scheduler.ts`)が `idle-exit` を返してループが終了する。
- 掘り終えた系統: 利用者から見た CLI の体験 / ループの運転そのものの弱点 / 初回導入の体験 /
  並列実行と状態ディレクトリ / 長時間の連続稼働 / 複数リポジトリの併用 / 費用・所要時間 /
  記録ファイルの読み書きの頑健性 / 失敗時の診断体験 / テスト自体の質 / 探索とタスクの並走 /
  Claude Code プロセスの起動と結果の解釈 / セッションに渡す設定の組み立て / `ccloop status`・`watch` の
  表示ロジック / `.agent/` 雛形と実リポジトリの往復 / git 操作の異常系 / エスカレーション・リトライ方針 /
  作業場所とタスクの対応が崩れたときの回復 / `.agent/` の記録が人手で編集されたときの扱い /
  多段処理の中断耐性 / 失敗回数の計上 / スケジューラの待機と終了の判断。
- 未検証のまま残った枝葉(実害は薄いと判断): submodule を含むリポジトリでの worktree 作成、
  shallow clone での `merge-base`、`.gitattributes` / `core.autocrlf` が退避パッチの復元に与える影響、
  `index.lock` 残存時の挙動、dirty な worktree を再利用したときの実挙動のテスト。

## 次にやると完了に近づくこと

1. `conflicts` の 2 タスク(仕組み → 指示)を直列で消化する。これで衝突対策が人力運用から仕組みへ移る。
2. `-0829` を「確認して completed にする」だけで閉じる(実装のやり直しは不要)。
3. 決定 15 件の承認と退避ブランチの削除(人間の手)。

次の探索セッションへの申し送り:

- **確認トピックの枠は空いた(0/4)が、在庫も空。** 出せるネタが見つかったら提出してよいが、
  無理にひねり出さないこと。見送り済みトピック(上記)は再提案しない。
- **`lib/supervisor.ts` を触るタスクを 2 件以上同時に ready にするなら直列化すること。**
  `conflicts` の実装が入るまでは `dependencies` で張る(入った後は `conflicts` を使う)。
  `CHANGELOG.md` の「## 未リリース」節も取り合いが起きる。
- **要対応欄に出ている残務を「修理ネタ」と取り違えないこと。** 退避ブランチの「未取り込み」表示は
  設計どおりであり、決定の承認は人間の操作待ちである。
- 走行中のタスク(ブランチ `agent/T-*` が存在するもの)のファイルは編集しない。
- 調査で `Bash(git worktree*)` とブランチ操作が権限で使えないため、作業場所まわりは実機再現ができない。
  コード読解で当たりを付け、再現は着手するタスクセッション側に委ねる形になる。
